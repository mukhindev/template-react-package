{
  "extends": ["stylelint-config-standard"],
  "rules": {
    "selector-class-pattern": [
      "^root$|^__[a-z]+$|^_[a-z]+_?[a-z0-9]+?$",
      {
        "message": "Support class naming: `.root`, `.__element`, `._modifier', `._modifier_value`"
      }
    ]
  }
}
